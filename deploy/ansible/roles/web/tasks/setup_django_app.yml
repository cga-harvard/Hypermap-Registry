---

- name: Install packages required by the Django app inside virtualenv
  pip: virtualenv={{ virtualenv_path }} name={{ project_path }} state=present

- name: Install Mapproxy 
  pip: virtualenv={{ virtualenv_path }} name=MapProxy version=1.10.0a0.dev20160928
       extra_args='-i https://testpypi.python.org/pypi/' state=present

- name: Run the Django syncdb command
  django_manage:
    command: syncdb
    app_path: "{{ project_path }}/{{ project_name }}"
    virtualenv: "{{ virtualenv_path }}"
    settings: "{{ django_settings_file }}"
  environment: "{{ django_environment }}"
  tags: django

- name: Load initial admin user
  django_manage:
    command: "loaddata aggregator/fixtures/user.json"
    app_path: "{{ project_path }}/{{ project_name }}"
    virtualenv: "{{ virtualenv_path }}"
    settings: "{{ django_settings_file }}"
  environment: "{{ django_environment }}"
  tags: django

- name: Run Django collectstatic
  django_manage:
    command: collectstatic
    app_path: "{{ project_path }}/{{ project_name }}"
    virtualenv: "{{ virtualenv_path }}"
    settings: "{{ django_settings_file }}"
  environment: "{{ django_environment }}"
  tags: django
